# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  - package-ecosystem: "pip" # See documentation for possible values
    directory: "/" # Location of package manifests
    schedule:
      interval: "daily"

  # The workflows' actions were not watched by anything, which is how they sat
  # on the Node 20 runtime until GitHub deprecated it. Weekly, because a major
  # bump here is a workflow change to read rather than a lockfile line.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
